<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Contoh HTML Dasar — Halaman Satu</title>
  <meta name="description" content="Contoh halaman HTML dasar dengan CSS responsif.">
  <style>
    /* Reset kecil */
    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%}
    body{font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;line-height:1.5;color:#111;background: linear-gradient(180deg,#f7fbff,#ffffff);}

    /* Layout */
    .container{max-width:1000px;margin:28px auto;padding:20px}

    header{display:flex;justify-content:space-between;align-items:center;margin-bottom:20px}
    .brand{font-weight:700;font-size:1.25rem}
    nav a{margin-left:14px;text-decoration:none;color:#0366d6}

    /* Hero */
    .hero{background:#ffffff;border-radius:12px;padding:24px;box-shadow:0 6px 18px rgba(11,22,55,0.06);display:grid;grid-template-columns:1fr 280px;gap:18px;align-items:center}
    .hero h1{font-size:1.6rem;margin-bottom:8px}
    .hero p{color:#334155}
    .cta{display:inline-block;margin-top:12px;padding:10px 14px;border-radius:10px;background:#0366d6;color:#fff;text-decoration:none}

    /* Cards */
    .cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:16px;margin:18px 0}
    .card{background:#fff;padding:14px;border-radius:10px;box-shadow:0 4px 12px rgba(11,22,55,0.04)}
    .card h3{margin-bottom:8px;font-size:1.05rem}
    .card p{font-size:0.95rem;color:#475569}

    /* Form */
    form{background:#fff;padding:14px;border-radius:10px;box-shadow:0 6px 20px rgba(11,22,55,0.04)}
    label{display:block;margin-bottom:6px;font-weight:600}
    input,textarea{width:100%;padding:10px;border:1px solid #e6eef8;border-radius:8px;margin-bottom:12px}
    button{background:#059669;color:#fff;padding:10px 14px;border-radius:8px;border:0;cursor:pointer}

    /* Footer */
    footer{margin-top:20px;text-align:center;color:#64748b;font-size:0.9rem}

    /* Responsif */
    @media (max-width:720px){
      .hero{grid-template-columns:1fr}
      header{flex-direction:column;align-items:flex-start}
      nav{margin-top:8px}
    }

    /* Interaktivitas CSS kecil: hover */
    .card:hover{transform:translateY(-6px);transition:transform .2s}
    .cta:hover{opacity:.9}
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="brand">HTML Dasar</div>
      <nav>
        <a href="#features"